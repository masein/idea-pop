global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: "idea-pop-api"
    static_configs:
      - targets: ["backend:8080"]
    metrics_path: /metrics
